name: farm_marketplace_shared_api
description: Shared API integration layer for seller and buyer apps.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
